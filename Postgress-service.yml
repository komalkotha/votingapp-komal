apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    name: redis-service
    app: demo-voting-app
spec:
  ports:
    - port: 5432
      targetport: 5432
selector:
  name: postgress-pod
  app: demo-voting-app