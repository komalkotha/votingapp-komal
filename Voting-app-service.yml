apiVersion: v1
kind: Service
metadata:
  name: Voting-service
  labels:
    name: Voting-service
    app: demo-voting-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
selector:
  name: postgress-pod
  app: demo-voting-app