appVersion: v1
kind: pod
metadata:
  name: voting-app
  labels:
    name: voting-app-pod
    app: demo-voting-app-pod
spec:
  containers:
    - name: voting-app
      image: dockersamples/examplevotingapp_vote
      ports:
        - containerPort: 80